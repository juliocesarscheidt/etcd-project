apiVersion: v1
kind: Service
metadata:
  name: etcd-app-svc
  namespace: development
  labels:
    app: etcd-app

spec:
  type: ClusterIP

  selector:
    app: etcd-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: etcd-app-port
